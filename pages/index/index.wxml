<!-- 轮播 -->
 <xy-banner bind:bannerClick="bannerClick" location="{{1}}" bannerList="{{bannerList}}"/>

 <view class="list" >
  <!-- 导航 -->
  <view class="list-tab flex-cs">
    <view class="list-tab_left" bindtap="text">当前培训和测试</view>
    <navigator class="list-tab_right" url="/ch-course/pages/allCourse/allCourse">
      查看更多
      <image src="/assets/images/common/right.png"></image>
    </navigator>
  </view>
  <!-- 列表 -->
  <view>
    <block wx:for="{{dataList}}" wx:key="index">
      <xy-list  list="{{item}}" data-type="skill" data-id="{{item.id}}" bindtap="educationClick"/>
    </block>
  </view>

 </view>
